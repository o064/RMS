#include "models/SeatAllocator.h"
#include <iostream>

using namespace  std;
int main(int argc, char **argv)
{
//    SeatAllocator* sa = new SeatAllocator(-1);
//    sa->printStatus();
//    std::cout << sa->hasAvailableSeats() << endl;
//    sa->allocateSeat(10);
//     sa->allocateSeat(20);
//     cout << sa->getAvailableSeatCount() <<endl;
//     sa->freeSeat(2);
//    sa->freeSeat(23);
//    sa->freeSeat(10);
//    sa->freeSeat(-1);
//    sa->allocateSeat(20);
//    sa->allocateSeat(20);
//    sa->allocateSeat(20);
//    cout << sa->getAvailableSeatCount() <<endl;
//    cout << sa->hasAvailableSeats() <<endl;
//    sa->allocateSeat(20);
//    sa->allocateSeat(20);
//    sa->allocateSeat(20);
//    sa->allocateSeat(20);
//    sa->printStatus();
    return 0;
}